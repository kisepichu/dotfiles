# Runtime artifacts of the permission-supervisor hook (audit log + live state).
# These are generated at run time and must never be committed or chezmoi-managed.
dot_claude/hooks/supervisor/logs/

# Claude Code per-repo runtime state (created when running Claude Code here).
.claude/scheduled_tasks.lock


tmp
