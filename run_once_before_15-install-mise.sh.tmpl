{{- if eq .chezmoi.os "linux" -}}
#!/usr/bin/env bash
set -euo pipefail

if command -v mise >/dev/null 2>&1; then
  exit 0
fi

curl -fsSL https://mise.run | sh
{{- else -}}
#!/usr/bin/env bash
set -euo pipefail
exit 0
{{- end }}
